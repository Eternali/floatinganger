class Trailer {

  constructor({
    target,
    offset,
    length,
    fidelity
  }) {
    this.target = target;
    this.offset = offset;
    this.length = length;
    this.fidelity = fidelity;
  }

  init(scene) {  }
  advance() {  }

}